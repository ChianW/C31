# Runtime memory — never commit personal data
# (only excludes root-level dirs, not agents/memory/ templates)
/memory/
/.memory/
/diary/
/session_state.json
/instincts/
*.session.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
